---
phase: 04-multi-agent-core
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/app/src/main/index.ts
  - apps/desktop/app/src/main/agent-registry.ts
  - apps/desktop/app/src/preload/index.ts
  - apps/desktop/app/src/renderer/App.tsx
  - apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx
  - apps/desktop/app/src/renderer/components/AgentDetailPanel.test.tsx
  - apps/desktop/app/src/renderer/App.test.ts
autonomous: true
gap_closure: true
requirements: [AGENT-01, AGENT-05, AGENT-14]
must_haves:
  truths:
    - "User can delete an agent from the active registry UI and the hierarchy refreshes immediately."
    - "User can edit any agent-specific doc in the right panel and the saved content persists after reload."
    - "Agent CRUD in the active app tree is fully wired through renderer, preload, and main IPC."
  artifacts:
    - path: "apps/desktop/app/src/main/index.ts"
      provides: "agent:delete IPC handler and persisted doc update path"
    - path: "apps/desktop/app/src/preload/index.ts"
      provides: "deleteAgent renderer bridge"
    - path: "apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx"
      provides: "doc editor that calls a persistence callback instead of resetting local draft only"
    - path: "apps/desktop/app/src/renderer/App.tsx"
      provides: "delete flow, doc-save flow, and hierarchy reload after mutations"
  key_links:
    - from: "apps/desktop/app/src/renderer/App.tsx"
      to: "apps/desktop/app/src/preload/index.ts"
      via: "window.clawhive.deleteAgent and window.clawhive.updateAgent"
      pattern: "window\\.clawhive\\.(deleteAgent|updateAgent)"
    - from: "apps/desktop/app/src/preload/index.ts"
      to: "apps/desktop/app/src/main/index.ts"
      via: "agent:delete IPC channel"
      pattern: "agent:delete"
    - from: "apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx"
      to: "apps/desktop/app/src/renderer/App.tsx"
      via: "onSaveAgentDoc callback"
      pattern: "onSaveAgentDoc"
---

<objective>
Close the verified registry gap in the active desktop app by finishing end-to-end delete and agent-doc persistence.

Purpose: make the existing registry UI real instead of partially stubbed.
Output: active-app delete flow, persisted agent doc editing, and regression tests proving both behaviors.
</objective>

<execution_context>
@/Users/stevenyxz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyxz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-agent-core/04-VERIFICATION.md
@.planning/phases/04-multi-agent-core/04-02-CONTEXT.md
@.planning/phases/04-multi-agent-core/04-02-01-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-02-04-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-01-SUMMARY.md

<interfaces>
From apps/desktop/app/src/main/agent-registry.ts:
```ts
export class AgentRegistry {
  updateAgent(id: string, updates: Partial<AgentRecord>): AgentRecord
  deleteAgent(id: string): void
  getHierarchy(): TreeNode<AgentRecord>[]
}
```

From apps/desktop/app/src/common/agent.ts:
```ts
export const AGENT_DOC_KEYS = ['soul', 'heartbeat', 'tools', 'agents', 'interaction'] as const
export interface AgentRecord {
  id: string
  name: string
  role: AgentRole
  docs: AgentDocs
  status: AgentStatus
  lifecycle: AgentLifecycle
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose real delete + doc persistence in the active IPC stack</name>
  <read_first>
    - apps/desktop/app/src/main/index.ts
    - apps/desktop/app/src/main/agent-registry.ts
    - apps/desktop/app/src/preload/index.ts
    - apps/desktop/app/src/common/agent.ts
  </read_first>
  <files>apps/desktop/app/src/main/index.ts, apps/desktop/app/src/main/agent-registry.ts, apps/desktop/app/src/preload/index.ts</files>
  <action>Add `ipcMain.handle('agent:delete', (_, id: string) => { agentRegistry.deleteAgent(id); return { success: true, id } })` to the main process, and expose `deleteAgent: (id: string) => ipcRenderer.invoke('agent:delete', id)` from preload. Keep doc persistence on the existing `agent:update` path by updating agents with a full `docs` object shaped as `{ soul, heartbeat, tools, agents, interaction }`; do not add a second doc-only channel. If delete needs cleanup beyond the database row, keep it inside `AgentRegistry.deleteAgent()` so renderer code never reaches into storage details.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/main/index.ts` contains the exact string `ipcMain.handle('agent:delete'`
    - `apps/desktop/app/src/preload/index.ts` contains the exact string `deleteAgent: (id: string)`
    - `apps/desktop/app/src/main/index.ts` still contains `ipcMain.handle('agent:update'` and the updated path accepts `docs`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/renderer/App.test.ts src/renderer/components/AgentDetailPanel.test.tsx</automated>
  </verify>
  <done>Main/preload wiring exists for `agent:delete`, and the persisted agent update path can carry edited docs without adding a second persistence seam.</done>
</task>

<task type="auto">
  <name>Task 2: Replace renderer stubs with real delete and doc-save flows</name>
  <read_first>
    - apps/desktop/app/src/renderer/App.tsx
    - apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx
    - apps/desktop/app/src/preload/index.ts
    - apps/desktop/app/src/renderer/App.test.ts
  </read_first>
  <files>apps/desktop/app/src/renderer/App.tsx, apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx, apps/desktop/app/src/renderer/components/AgentDetailPanel.test.tsx, apps/desktop/app/src/renderer/App.test.ts</files>
  <action>In `App.tsx`, replace the `console.warn('Delete agent not implemented yet', id)` branch with an async delete flow that calls `window.clawhive.deleteAgent(id)`, clears `selectedSubjectId` / `rightPanelSubjectId` / `activeAgentId` when they reference the deleted agent, and then calls the existing loader so `agents` and `hierarchyNodes` refresh together. In `AgentDetailPanel.tsx`, remove the deferred save stub and add an `onSaveAgentDoc(agentId, docKey, content)` callback prop; when the user saves, build the next `docs` object from the current `agent.docs`, write the updated string into the matching key (`soul`, `heartbeat`, `tools`, `agents`, or `interaction`), await the callback, and leave the editor closed only after the promise resolves. Add/extend tests so one assertion checks `window.clawhive.deleteAgent(` is present in `App.tsx`, and another checks `AgentDetailPanel` contains `onSaveAgentDoc` plus no longer contains `Save will be wired to IPC in future task when doc persistence is added`.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/renderer/App.tsx` does not contain `Delete agent not implemented yet`
    - `apps/desktop/app/src/renderer/App.tsx` contains `window.clawhive.deleteAgent(`
    - `apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx` contains `onSaveAgentDoc`
    - `apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx` does not contain `Save will be wired to IPC in future task when doc persistence is added`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/renderer/App.test.ts src/renderer/components/AgentDetailPanel.test.tsx</automated>
  </verify>
  <done>Deleting an agent and saving any agent doc both use the active app IPC stack and survive a hierarchy reload.</done>
</task>

</tasks>

<verification>
Run the targeted renderer tests, then manually read the touched files to confirm the renderer no longer contains the delete/doc-persistence stubs called out in `04-VERIFICATION.md`.
</verification>

<success_criteria>
- Active app delete flow is fully wired through renderer, preload, and main process.
- Agent doc edits persist through `agent:update` and no longer reset local draft only.
- The exact blocker strings identified by verification are removed from the active app tree.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-agent-core/04-07-SUMMARY.md`
</output>
