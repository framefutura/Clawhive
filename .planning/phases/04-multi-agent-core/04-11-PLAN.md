---
phase: 04-multi-agent-core
plan: "11"
type: execute
wave: 2
depends_on: ["04-09", "04-10"]
files_modified:
  - apps/desktop/app/src/common/a2a.ts
  - apps/desktop/app/src/main/message-bus.ts
  - apps/desktop/app/src/main/message-bus.test.ts
  - apps/desktop/app/src/main/a2a-messaging.ts
  - apps/desktop/app/src/main/a2a-messaging.test.ts
  - apps/desktop/app/src/main/index.ts
  - apps/desktop/app/src/preload/index.ts
  - apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx
  - apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx
  - apps/desktop/app/src/renderer/components/LeaderMessageHub.test.tsx
  - apps/desktop/app/src/renderer/App.tsx
autonomous: true
gap_closure: true
requirements: [AGENT-29, AGENT-30, AGENT-31, AGENT-32, AGENT-33, AGENT-34, AGENT-35, AGENT-36, AGENT-37, AGENT-38]
must_haves:
  truths:
    - "Every A2A message in the active app is scanned by PrivacyGuard before delivery."
    - "Approval and guidance requests route through immediate leaders, can escalate up the chain, and can relay through the Secretary when configured."
    - "Leaders can inspect team-wide message traffic and coaching/guidance threads in a message hub in the active app."
  artifacts:
    - path: "apps/desktop/app/src/main/message-bus.ts"
      provides: "PrivacyGuard-backed A2A pipeline enforcement"
    - path: "apps/desktop/app/src/main/a2a-messaging.ts"
      provides: "leader routing, escalation chains, secretary relay, guidance, and coaching archive behavior"
    - path: "apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx"
      provides: "leader visibility UI for team-wide and per-agent message threads"
    - path: "apps/desktop/app/src/main/index.ts"
      provides: "IPC routes for escalation and message-hub queries"
  key_links:
    - from: "apps/desktop/app/src/main/message-bus.ts"
      to: "apps/desktop/app/src/main/privacy-guard.ts"
      via: "PrivacyGuard.detectSuspicious() in message validation"
      pattern: "detectSuspicious"
    - from: "apps/desktop/app/src/main/a2a-messaging.ts"
      to: "apps/desktop/app/src/main/agent-registry.ts"
      via: "leader-chain lookup"
      pattern: "parentId|getAgent|getHierarchy"
    - from: "apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx"
      to: "apps/desktop/app/src/preload/index.ts"
      via: "a2a escalation and hub bridge methods"
      pattern: "a2a"
---

<objective>
Close the remaining A2A scope gap by extending the active A2A foundation with escalation chains, secretary relay, leader hub visibility, PrivacyGuard scans, and parent-guidance/coaching flows.

Purpose: turn the current 4.05 foundation into the full declared Phase 04 messaging surface.
Output: PrivacyGuard-enforced A2A pipeline, escalation/guidance/coaching logic, and leader-facing message hub UI in the active app tree.
</objective>

<execution_context>
@/Users/stevenyxz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyxz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-agent-core/04-VERIFICATION.md
@.planning/phases/04-multi-agent-core/04-04-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-09-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-10-SUMMARY.md

<interfaces>
From apps/desktop/app/src/main/message-bus.ts:
```ts
export class MessageBus {
  async send(envelope: A2AEnvelope): Promise<SendResult>
  subscribe(agentId: string, handler: (msg: A2AMessage) => void): () => void
  getInbox(agentId: string): A2AMessage[]
}
```

From apps/desktop/app/src/main/a2a-messaging.ts:
```ts
export class A2AMessaging {
  async sendPrompt(fromAgentId: string, toAgentId: string, prompt: string, context?: string): Promise<string>
  async replyTo(fromAgentId: string, messageId: string, content: string): Promise<void>
  readContext(fromAgentId: string, toAgentId: string): AgentContextSnapshot | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce PrivacyGuard and escalation logic in the main A2A layer</name>
  <read_first>
    - apps/desktop/app/src/main/message-bus.ts
    - apps/desktop/app/src/main/a2a-messaging.ts
    - apps/desktop/app/src/main/privacy-guard.ts
    - apps/desktop/app/src/main/message-bus.test.ts
    - apps/desktop/app/src/main/a2a-messaging.test.ts
    - apps/desktop/app/src/common/a2a.ts
  </read_first>
  <files>apps/desktop/app/src/common/a2a.ts, apps/desktop/app/src/main/message-bus.ts, apps/desktop/app/src/main/message-bus.test.ts, apps/desktop/app/src/main/a2a-messaging.ts, apps/desktop/app/src/main/a2a-messaging.test.ts</files>
  <behavior>
    - Test 1: every A2A send path calls `PrivacyGuard.detectSuspicious()` and blocks suspicious messages before inbox delivery.
    - Test 2: approval-required requests route to the immediate leader first and escalate to the next superior when unresolved.
    - Test 3: stuck subagents can send guidance requests to their parent, and coaching/self-improvement messages are archived for later review.
  </behavior>
  <action>Extend the shared A2A contracts with explicit message types and metadata for `approval-request`, `approval-decision`, `guidance-request`, `guidance-response`, `coaching`, and `self-improvement`. In `message-bus.ts`, import the active app Privacy Guard and call `getPrivacyGuard().detectSuspicious(...)` for every envelope before content delivery; suspicious results must return `delivered: false` and write an audit-log denial entry. In `a2a-messaging.ts`, add leader-chain routing helpers that resolve immediate leader from agent hierarchy, climb superiors until a handler is found, respect a configurable CEO top-level destination of either `secretary` or `user`, and archive coaching/self-improvement messages in a retrievable in-memory or persisted store that later UI can query. Preserve the existing read-guard behavior from 04-04; do not bypass it for prompts.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/main/message-bus.ts` contains `detectSuspicious`
    - `apps/desktop/app/src/common/a2a.ts` contains `approval-request`
    - `apps/desktop/app/src/common/a2a.ts` contains `guidance-request`
    - `apps/desktop/app/src/main/a2a-messaging.ts` contains `secretary`
    - `apps/desktop/app/src/main/a2a-messaging.ts` contains `coaching`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/main/message-bus.test.ts src/main/a2a-messaging.test.ts</automated>
  </verify>
  <done>The active A2A layer now enforces PrivacyGuard and supports escalation, guidance, and coaching/self-improvement flows.</done>
</task>

<task type="auto">
  <name>Task 2: Expose escalation and message-hub visibility in the active app UI</name>
  <read_first>
    - apps/desktop/app/src/main/index.ts
    - apps/desktop/app/src/preload/index.ts
    - apps/desktop/app/src/renderer/App.tsx
    - apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx
    - apps/desktop/app/src/main/a2a-messaging.ts
  </read_first>
  <files>apps/desktop/app/src/main/index.ts, apps/desktop/app/src/preload/index.ts, apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx, apps/desktop/app/src/renderer/components/LeaderMessageHub.test.tsx, apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx, apps/desktop/app/src/renderer/App.tsx</files>
  <action>Add IPC handlers and preload methods for escalation submission, guidance reply, leader-hub query, and coaching archive query. Create `LeaderMessageHub.tsx` in the active renderer with filters labeled exactly `handoff`, `discussion`, `suggestion`, and `escalation`, plus team-wide and per-agent thread views. Wire the Secretary and CEO panels in `AgentDetailPanel.tsx` to live counts from the new A2A service instead of static zeros, and mount `LeaderMessageHub` from `App.tsx` for leaders so it fits the locked three-pane workspace. Do not leave `Pending Escalations`, `Decisions Relayed`, or CEO counters as hard-coded `0` values.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx` contains `escalation`
    - `apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx` contains `handoff`
    - `apps/desktop/app/src/renderer/components/LeaderMessageHub.tsx` contains `discussion`
    - `apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx` does not contain both static `Pending Escalations` and `<div>0</div>` in the Secretary surface
    - `apps/desktop/app/src/renderer/App.tsx` contains `LeaderMessageHub`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/main/message-bus.test.ts src/main/a2a-messaging.test.ts src/renderer/components/LeaderMessageHub.test.tsx</automated>
  </verify>
  <done>Leaders, Secretary, and CEO all have live escalation/message-hub surfaces in the active app tree.</done>
</task>

</tasks>

<verification>
The full A2A gap is closed when the main tests prove PrivacyGuard + escalation behavior and the renderer tests prove the leader hub and special-role panels are using live data rather than placeholders.
</verification>

<success_criteria>
- Every A2A message path is scanned by PrivacyGuard in the active app.
- Immediate-leader routing, superior escalation, secretary relay, leader message hub, guidance, and coaching flows all exist in active-root code.
- Static placeholder counters in Secretary/CEO surfaces are replaced with real queried values.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-agent-core/04-11-SUMMARY.md`
</output>
