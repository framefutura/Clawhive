---
phase: 04-multi-agent-core
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/app/src/renderer/components/OrgTree.tsx
  - apps/desktop/app/src/renderer/components/OrgTree.test.ts
  - apps/desktop/app/src/renderer/hooks/useDragReparent.ts
  - apps/desktop/app/src/renderer/hooks/useDragReparent.test.ts
  - apps/desktop/app/src/renderer/App.tsx
  - apps/desktop/app/src/renderer/App.test.ts
autonomous: true
gap_closure: true
requirements: [AGENT-02, AGENT-08, AGENT-09]
must_haves:
  truths:
    - "User can drag an agent onto a new parent in the active hierarchy UI instead of using only a move modal."
    - "The hierarchy switcher in the active org tree is either wired to state changes or removed so no inert control remains."
    - "Cycle-prevented reparenting still works across hierarchy, org-chart, and teams modes in the active app tree."
  artifacts:
    - path: "apps/desktop/app/src/renderer/components/OrgTree.tsx"
      provides: "drag-start, drag-over, drop, and active view-switch behavior"
    - path: "apps/desktop/app/src/renderer/hooks/useDragReparent.ts"
      provides: "cycle-aware drag-drop helper used by OrgTree"
    - path: "apps/desktop/app/src/renderer/App.tsx"
      provides: "single source of truth for view-mode changes and reparent callback"
  key_links:
    - from: "apps/desktop/app/src/renderer/components/OrgTree.tsx"
      to: "apps/desktop/app/src/renderer/hooks/useDragReparent.ts"
      via: "drag event handlers"
      pattern: "onDrag(Start|Over|Leave|End|Drop)"
    - from: "apps/desktop/app/src/renderer/components/OrgTree.tsx"
      to: "apps/desktop/app/src/renderer/App.tsx"
      via: "viewMode/onViewModeChange prop"
      pattern: "onViewModeChange"
    - from: "apps/desktop/app/src/renderer/components/OrgTree.tsx"
      to: "window.clawhive.updateAgent"
      via: "existing onReparentAgent callback chain"
      pattern: "onReparentAgent"
---

<objective>
Close the verified hierarchy-ui gap in the active desktop app by implementing true drag-drop reparenting and removing the inert org-tree switcher behavior.

Purpose: deliver the interaction model the user locked in CONTEXT.md instead of the modal-only fallback.
Output: drag-enabled org tree across all required modes, cycle-safe drop logic, and regression tests for the live control surface.
</objective>

<execution_context>
@/Users/stevenyxz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyxz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-agent-core/04-VERIFICATION.md
@.planning/phases/04-multi-agent-core/04-02-CONTEXT.md
@.planning/phases/04-multi-agent-core/04-RESEARCH.md
@.planning/phases/04-multi-agent-core/04-02-02-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-02-03-SUMMARY.md

<interfaces>
From apps/desktop/app/src/renderer/components/OrgTree.tsx:
```ts
interface OrgTreeProps {
  nodes: TreeNode<AgentRecord>[]
  allAgents: AgentRecord[]
  viewMode: 'hierarchy' | 'org-chart' | 'teams'
  onReparentAgent: (agentId: string, newParentId: string | undefined, comment: string) => Promise<{ success: boolean; error?: string }>
}
```

From apps/desktop/app/src/renderer/hooks/useDragReparent.ts:
```ts
export function useDragReparent(...)
export function wouldCreateCycle(agents: AgentRecord[], dragId: string, newParentId: string): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade the shared drag helper to support real reparent drops</name>
  <read_first>
    - apps/desktop/app/src/renderer/hooks/useDragReparent.ts
    - apps/desktop/app/src/renderer/hooks/useDragReparent.test.ts
    - apps/desktop/app/src/common/agent.ts
  </read_first>
  <files>apps/desktop/app/src/renderer/hooks/useDragReparent.ts, apps/desktop/app/src/renderer/hooks/useDragReparent.test.ts</files>
  <behavior>
    - Test 1: dropping an agent onto itself or any descendant returns a blocked result and preserves the cycle reason.
    - Test 2: dropping an agent onto a valid new parent returns an allowed result with the exact `newParentId` used for persistence.
    - Test 3: clearing drag state resets any previous hovered drop target.
  </behavior>
  <action>Expand `useDragReparent` from a pure cycle helper into a drag-state hook for the active app tree. Export a result shape that includes `draggedAgentId`, `hoverParentId`, `canDrop`, `reason`, `startDrag(agentId)`, `setHoverParent(parentId | undefined)`, `completeDrop(newParentId | undefined)`, and `cancelDrag()`. Keep `wouldCreateCycle()` as the same O(n) ancestor walk described in `04-RESEARCH.md`; do not add quadratic scans or modal-only fallbacks. Return the exact rejection reason string `Circular reference detected` when the candidate parent is a descendant so it lines up with existing hierarchy validation language.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/renderer/hooks/useDragReparent.ts` contains `startDrag(`
    - `apps/desktop/app/src/renderer/hooks/useDragReparent.ts` contains `completeDrop(`
    - `apps/desktop/app/src/renderer/hooks/useDragReparent.ts` contains the exact string `Circular reference detected`
    - `apps/desktop/app/src/renderer/hooks/useDragReparent.test.ts` contains `completeDrop` coverage
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/renderer/hooks/useDragReparent.test.ts</automated>
  </verify>
  <done>The drag helper can drive real drop interactions and proves cycle blocking with automated tests before any UI wiring is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire actual drag/drop and live view switching into OrgTree</name>
  <read_first>
    - apps/desktop/app/src/renderer/components/OrgTree.tsx
    - apps/desktop/app/src/renderer/components/OrgTree.test.ts
    - apps/desktop/app/src/renderer/App.tsx
    - apps/desktop/app/src/renderer/App.test.ts
    - apps/desktop/app/src/renderer/hooks/useDragReparent.ts
    - apps/desktop/app/src/renderer/components/hierarchy/HierarchyViewSwitcher.tsx
  </read_first>
  <files>apps/desktop/app/src/renderer/components/OrgTree.tsx, apps/desktop/app/src/renderer/components/OrgTree.test.ts, apps/desktop/app/src/renderer/App.tsx, apps/desktop/app/src/renderer/App.test.ts</files>
  <behavior>
    - Test 1: OrgTree renders drag handlers (`draggable`, `onDragStart`, `onDrop`) on agent rows and calls the existing reparent callback after a valid drop.
    - Test 2: the org-tree-level switcher is no longer inert; it either calls a real `onViewModeChange` prop or is removed from OrgTree entirely.
    - Test 3: all three view modes still render and route through the same drag/drop affordance instead of regressing to modal-only movement.
  </behavior>
  <action>Add an `onViewModeChange: (mode: 'hierarchy' | 'org-chart' | 'teams') => void` prop to `OrgTree` and pass `setHierarchyViewMode` from `App.tsx` so the inner switcher no longer uses `onChange={() => {}}`; if the duplicated switcher creates UX clutter, remove the duplicated inner switcher and keep the outer working switcher only, but there must be no inert control left in the file. In `OrgTree.tsx`, mark each rendered agent row as draggable, call `startDrag(agent.id)` on drag start, highlight candidate parents on drag over, call `completeDrop(targetAgentId)` on drop, and forward the returned parent id to the existing async `onReparentAgent(agentId, newParentId, '')` callback. Keep guided validation behavior by showing inline drop feedback or the existing result dialog for rejected moves; do not fall back to the `Move Agent` modal as the primary interaction. Preserve the three distinct render branches (`hierarchy`, `org-chart`, `teams`) required by the locked decision, and ensure each branch uses the shared drag-enabled row/card surface.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/renderer/components/OrgTree.tsx` contains `draggable`
    - `apps/desktop/app/src/renderer/components/OrgTree.tsx` contains at least one of `onDragStart`, `onDrop`, and `onDragOver`
    - `apps/desktop/app/src/renderer/components/OrgTree.tsx` does not contain `onChange={() => {}}`
    - `apps/desktop/app/src/renderer/App.tsx` contains `onViewModeChange={setHierarchyViewMode}` or `OrgTree` no longer renders an internal `HierarchyViewSwitcher`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/renderer/hooks/useDragReparent.test.ts src/renderer/components/OrgTree.test.ts src/renderer/App.test.ts</automated>
  </verify>
  <done>The active app tree supports actual drag/drop reparenting and no inert hierarchy switcher remains.</done>
</task>

</tasks>

<verification>
Use the targeted renderer tests to prove the drag helper and org tree wiring exist, then inspect `OrgTree.tsx` to confirm the primary reparent interaction is drag/drop rather than only `ReparentDialog`.
</verification>

<success_criteria>
- The active app hierarchy surface exposes real drag/drop handlers.
- No inert `HierarchyViewSwitcher` callback remains in `OrgTree.tsx`.
- All three required hierarchy modes remain available after drag/drop wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-agent-core/04-08-SUMMARY.md`
</output>
