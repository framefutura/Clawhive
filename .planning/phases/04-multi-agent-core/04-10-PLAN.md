---
phase: 04-multi-agent-core
plan: "10"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/app/src/common/team.ts
  - apps/desktop/app/src/main/team-manager.ts
  - apps/desktop/app/src/main/team-manager.test.ts
  - apps/desktop/app/src/main/index.ts
  - apps/desktop/app/src/preload/index.ts
  - apps/desktop/app/src/renderer/components/SwarmView.tsx
  - apps/desktop/app/src/renderer/components/SwarmView.test.tsx
  - apps/desktop/app/src/renderer/components/TeamWorkspace.tsx
  - apps/desktop/app/src/renderer/components/TeamWorkspace.test.tsx
  - apps/desktop/app/src/renderer/App.tsx
autonomous: true
gap_closure: true
requirements: [AGENT-21, AGENT-22, AGENT-23, AGENT-24, AGENT-25, AGENT-26, AGENT-27]
must_haves:
  truths:
    - "The active app exposes team workspace and shared/private memory instead of leaving team code in the inactive root."
    - "Users can selectively share conversations into team memory from the active chat workflow."
    - "Leaders can see monitoring, coaching, and OKR information for their teams inside the active app tree."
  artifacts:
    - path: "apps/desktop/app/src/main/team-manager.ts"
      provides: "team CRUD, shared/private memory behavior, coaching, and OKR state in active app root"
    - path: "apps/desktop/app/src/renderer/components/SwarmView.tsx"
      provides: "leader monitoring surface in active app root"
    - path: "apps/desktop/app/src/renderer/components/TeamWorkspace.tsx"
      provides: "active chat sharing and shared memory UI in active app root"
    - path: "apps/desktop/app/src/main/index.ts"
      provides: "team IPC handlers wired to active app main process"
  key_links:
    - from: "apps/desktop/app/src/renderer/components/TeamWorkspace.tsx"
      to: "apps/desktop/app/src/preload/index.ts"
      via: "team share/query methods"
      pattern: "team"
    - from: "apps/desktop/app/src/main/team-manager.ts"
      to: "apps/desktop/app/src/main/agent-storage.ts"
      via: "team storage isolation"
      pattern: "createTeamStorageDir|addAgentToTeamStorage"
    - from: "apps/desktop/app/src/renderer/App.tsx"
      to: "apps/desktop/app/src/renderer/components/(SwarmView|TeamWorkspace).tsx"
      via: "active-shell integration"
      pattern: "SwarmView|TeamWorkspace"
---

<objective>
Close the team-collaboration gap by porting the verified orphaned team manager work into the active desktop app tree and extending it with monitoring, coaching, and OKRs.

Purpose: remove split-root drift and make Phase 4.04 real in `apps/desktop/app/src/`.
Output: active-root team manager main/renderer/preload integration, selective sharing from active chat flows, and leader monitoring/coaching/OKR surfaces.
</objective>

<execution_context>
@/Users/stevenyxz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyxz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-agent-core/04-VERIFICATION.md
@.planning/phases/04-multi-agent-core/04-03-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-06-SUMMARY.md

<interfaces>
From apps/desktop/src/main/team-manager.ts:
```ts
export class TeamManager {
  createTeam(name: string, leaderId: string, department?: string): Promise<TeamRecord>
  addMember(teamId: string, agentId: string): Promise<void>
  shareMemory(teamId: string, agentId: string, memory: ...): SharedMemory
  queryMemory(teamId: string, query: string, tags?: string[]): SharedMemory[]
}
```

From apps/desktop/src/renderer/components/SwarmView.tsx and TeamWorkspace.tsx:
```ts
export function SwarmView(props: SwarmViewProps)
export function TeamWorkspace(props: TeamWorkspaceProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port team contracts and main-process manager into the active app root</name>
  <read_first>
    - apps/desktop/src/main/team-manager.ts
    - apps/desktop/src/renderer/components/SwarmView.tsx
    - apps/desktop/src/renderer/components/TeamWorkspace.tsx
    - apps/desktop/app/src/main/index.ts
    - apps/desktop/app/src/main/agent-storage.ts
    - apps/desktop/app/src/common/agent.ts
  </read_first>
  <files>apps/desktop/app/src/common/team.ts, apps/desktop/app/src/main/team-manager.ts, apps/desktop/app/src/main/team-manager.test.ts, apps/desktop/app/src/main/index.ts, apps/desktop/app/src/preload/index.ts</files>
  <behavior>
    - Test 1: team creation in the active app root creates shared storage and leader membership.
    - Test 2: shared/private memory queries distinguish team-shared records from agent-private records.
    - Test 3: coaching notes and OKR entries can be created and listed for a team leader dashboard.
  </behavior>
  <action>Copy the functional seams from `apps/desktop/src/main/team-manager.ts` into `apps/desktop/app/src/main/team-manager.ts`, but target only the active app imports and types. Create `apps/desktop/app/src/common/team.ts` with exact shared types for `TeamRecord`, `SharedMemory`, `CoachingEntry`, and `TeamOkr`, including fields `minSecurityLevel`, `sharedScope`, `leaderId`, `objective`, `keyResults`, and `reviewCadence`. Extend the active main-process team manager with methods for `createCoachingEntry`, `listCoachingEntries`, `createOkr`, and `listOkrs`; keep storage isolation routed through the active app `agent-storage.ts`. Add IPC handlers under the `team:*` namespace and matching preload methods so the renderer never imports the orphaned `apps/desktop/src` code.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/main/team-manager.ts` exists and contains `createCoachingEntry`
    - `apps/desktop/app/src/main/team-manager.ts` contains `createOkr`
    - `apps/desktop/app/src/main/index.ts` contains `team:create`
    - `apps/desktop/app/src/preload/index.ts` contains `createTeam:`
    - no new file in this plan uses the inactive root prefix `apps/desktop/src/`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/main/team-manager.test.ts</automated>
  </verify>
  <done>The active app tree owns team contracts, team manager behavior, and IPC integration, including monitoring/coaching/OKR data seams.</done>
</task>

<task type="auto">
  <name>Task 2: Port and wire team workspace + swarm UI into the active renderer shell</name>
  <read_first>
    - apps/desktop/src/renderer/components/SwarmView.tsx
    - apps/desktop/src/renderer/components/TeamWorkspace.tsx
    - apps/desktop/app/src/renderer/App.tsx
    - apps/desktop/app/src/renderer/components/ChatView.tsx
    - apps/desktop/app/src/preload/index.ts
  </read_first>
  <files>apps/desktop/app/src/renderer/components/SwarmView.tsx, apps/desktop/app/src/renderer/components/SwarmView.test.tsx, apps/desktop/app/src/renderer/components/TeamWorkspace.tsx, apps/desktop/app/src/renderer/components/TeamWorkspace.test.tsx, apps/desktop/app/src/renderer/App.tsx</files>
  <action>Port `SwarmView.tsx` and `TeamWorkspace.tsx` into `apps/desktop/app/src/renderer/components/` and wire them into `App.tsx` so the active shell can open a team workspace surface without depending on the inactive root. Preserve selective sharing from the active chat workflow by passing current chat messages/session context into `TeamWorkspace` and calling the new `window.clawhive.team*` preload methods. Extend the active renderer surfaces with explicit leader monitoring, coaching, and OKR sections labeled `Monitoring`, `Coaching Loop`, and `OKRs`; do not leave these as future placeholders. Keep the three-pane workspace decision intact by mounting team content in the middle/right panes rather than replacing the shell.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/renderer/components/TeamWorkspace.tsx` contains `Monitoring`
    - `apps/desktop/app/src/renderer/components/TeamWorkspace.tsx` contains `Coaching Loop`
    - `apps/desktop/app/src/renderer/components/TeamWorkspace.tsx` contains `OKRs`
    - `apps/desktop/app/src/renderer/App.tsx` contains `SwarmView`
    - `apps/desktop/app/src/renderer/App.tsx` contains `TeamWorkspace`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/main/team-manager.test.ts src/renderer/components/SwarmView.test.tsx src/renderer/components/TeamWorkspace.test.tsx</automated>
  </verify>
  <done>Team collaboration, selective sharing, monitoring, coaching, and OKR surfaces all exist in the active renderer tree.</done>
</task>

</tasks>

<verification>
Read the active-root team files after implementation and confirm the orphaned `apps/desktop/src/...` path is no longer the only substantive implementation for team behavior.
</verification>

<success_criteria>
- Team manager behavior lives in `apps/desktop/app/src/main/` and is reachable from preload/renderer.
- Shared/private memory and selective conversation sharing run through the active app tree.
- Leader monitoring, coaching, and OKR surfaces exist in the active renderer shell.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-agent-core/04-10-SUMMARY.md`
</output>
