---
phase: 04-multi-agent-core
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/app/src/common/task-router.ts
  - apps/desktop/app/src/main/task-router.ts
  - apps/desktop/app/src/main/task-router.test.ts
  - apps/desktop/app/src/main/index.ts
  - apps/desktop/app/src/preload/index.ts
  - apps/desktop/app/src/renderer/App.tsx
  - apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx
  - apps/desktop/app/src/renderer/components/TaskRouterPanel.test.tsx
autonomous: true
gap_closure: true
requirements: [AGENT-16, AGENT-17, AGENT-18, AGENT-19, AGENT-20]
must_haves:
  truths:
    - "Each agent in the active app can have its own heartbeat interval and busy agents are skipped instead of double-assigned."
    - "A parent task can auto-delegate to an appropriate child or team, and the user can override that choice from the active app UI."
    - "Delegation records carry originatingSecurityLevel and cap child execution at the minimum allowed level through the chain."
  artifacts:
    - path: "apps/desktop/app/src/main/task-router.ts"
      provides: "heartbeat scheduling, delegation, workload balancing, and security propagation"
    - path: "apps/desktop/app/src/common/task-router.ts"
      provides: "shared task-router contracts used by main, preload, and renderer"
    - path: "apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx"
      provides: "user override surface for choosing agent or team"
    - path: "apps/desktop/app/src/main/index.ts"
      provides: "IPC handlers for task-router create/run/status operations"
  key_links:
    - from: "apps/desktop/app/src/main/task-router.ts"
      to: "apps/desktop/app/src/main/agent-registry.ts"
      via: "hierarchy lookup for delegation"
      pattern: "getHierarchy|listAgents"
    - from: "apps/desktop/app/src/main/task-router.ts"
      to: "apps/desktop/app/src/main/a2a-messaging.ts"
      via: "pending task pickup and prompt handoff"
      pattern: "getPendingTasks|sendPrompt|consumePendingTask"
    - from: "apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx"
      to: "apps/desktop/app/src/preload/index.ts"
      via: "taskRouter* bridge methods"
      pattern: "taskRouter"
---

<objective>
Close the missing task-router slice of Phase 04 by building heartbeat scheduling, hierarchy delegation, user override, workload balancing, and security propagation in the active app tree.

Purpose: make multi-agent routing real instead of leaving A2A pending-task comments without an orchestrator.
Output: shared contracts, main-process task router, renderer override panel, and tests for all five router requirements.
</objective>

<execution_context>
@/Users/stevenyxz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyxz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-agent-core/04-VERIFICATION.md
@.planning/phases/04-multi-agent-core/04-RESEARCH.md
@.planning/phases/04-multi-agent-core/04-03-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-04-SUMMARY.md
@.planning/phases/04-multi-agent-core/04-06-SUMMARY.md

<interfaces>
From apps/desktop/app/src/main/a2a-messaging.ts:
```ts
export function getPendingTasks(agentId: string): A2AMessage[]
export function consumePendingTask(agentId: string, messageId: string): void
export function isAgentActive(agentId: string): boolean
```

From apps/desktop/app/src/main/agent-registry.ts:
```ts
export class AgentRegistry {
  listAgents(): AgentRecord[]
  getHierarchy(): TreeNode<AgentRecord>[]
  getDescendants(agentId: string): AgentRecord[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define task-router contracts and prove routing logic with tests first</name>
  <read_first>
    - apps/desktop/app/src/main/a2a-messaging.ts
    - apps/desktop/app/src/main/agent-registry.ts
    - apps/desktop/app/src/common/agent.ts
    - .planning/phases/04-multi-agent-core/04-VERIFICATION.md
  </read_first>
  <files>apps/desktop/app/src/common/task-router.ts, apps/desktop/app/src/main/task-router.test.ts</files>
  <behavior>
    - Test 1: a busy agent heartbeat tick is skipped and does not consume pending work.
    - Test 2: auto-delegation picks a valid child based on hierarchy and lowest active workload count.
    - Test 3: a user override can target an explicit agent or team and wins over auto-delegation.
    - Test 4: the resulting delegation record carries `originatingSecurityLevel` and `effectiveSecurityLevel = min(parentLevel, childLevel)`.
  </behavior>
  <action>Create `apps/desktop/app/src/common/task-router.ts` with concrete shared types: `TaskSecurityLevel = 'high' | 'medium' | 'low'`, `TaskAssignmentMode = 'auto' | 'agent' | 'team'`, `TaskRouteRequest`, `TaskRouteDecision`, `HeartbeatConfig`, and `TaskRouterSnapshot`. Include exact fields `originatingSecurityLevel`, `effectiveSecurityLevel`, `assignedAgentId`, `assignedTeamId`, `workloadCount`, `heartbeatIntervalMs`, and `skippedBecauseBusy`. In `apps/desktop/app/src/main/task-router.test.ts`, write failing tests against a not-yet-created `TaskRouter` class that verify skip-if-busy, hierarchy delegation, explicit override routing, workload balancing by active-task count, and security-level min propagation. Do not write production implementation in this task.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/common/task-router.ts` contains `originatingSecurityLevel`
    - `apps/desktop/app/src/common/task-router.ts` contains `effectiveSecurityLevel`
    - `apps/desktop/app/src/main/task-router.test.ts` contains `skippedBecauseBusy`
    - `apps/desktop/app/src/main/task-router.test.ts` contains `workloadCount`
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 within this plan must create `apps/desktop/app/src/main/task-router.ts` before the test command can pass</automated>
  </verify>
  <done>The contracts and failing tests fully describe AGENT-16 through AGENT-20 without requiring executor interpretation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the main-process task router and IPC surface</name>
  <read_first>
    - apps/desktop/app/src/common/task-router.ts
    - apps/desktop/app/src/main/task-router.test.ts
    - apps/desktop/app/src/main/index.ts
    - apps/desktop/app/src/main/a2a-messaging.ts
    - apps/desktop/app/src/main/agent-registry.ts
  </read_first>
  <files>apps/desktop/app/src/main/task-router.ts, apps/desktop/app/src/main/index.ts, apps/desktop/app/src/preload/index.ts</files>
  <action>Create `TaskRouter` in `apps/desktop/app/src/main/task-router.ts` with concrete methods `setHeartbeat(agentId, heartbeatIntervalMs)`, `enqueueTask(request: TaskRouteRequest)`, `tickAgent(agentId)`, `routeTask(request)`, and `getSnapshot()`. Use per-agent heartbeat intervals in milliseconds, call `isAgentActive(agentId)` to enforce the sql.js-style skip-if-busy guard, inspect hierarchy from `agentRegistry.getDescendants(parentId)` / `listAgents()` for auto-delegation, and choose the child with the smallest active task count for workload balancing. For overrides, support `mode: 'agent'` with `assignedAgentId` and `mode: 'team'` with `assignedTeamId`; do not silently fall back to auto if an explicit override target is missing—return a structured error. Compute `effectiveSecurityLevel` exactly as the minimum of the parent/default route level and the target agent’s `defaultSecurityLevel`, and store the original value in `originatingSecurityLevel`. Add IPC handlers named `taskRouter:setHeartbeat`, `taskRouter:enqueue`, `taskRouter:tick`, and `taskRouter:getSnapshot`, then expose matching preload methods named `taskRouterSetHeartbeat`, `taskRouterEnqueue`, `taskRouterTick`, and `taskRouterGetSnapshot`.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/main/task-router.ts` contains `tickAgent(`
    - `apps/desktop/app/src/main/task-router.ts` contains `skippedBecauseBusy`
    - `apps/desktop/app/src/main/task-router.ts` contains `originatingSecurityLevel`
    - `apps/desktop/app/src/main/index.ts` contains `taskRouter:enqueue`
    - `apps/desktop/app/src/preload/index.ts` contains `taskRouterEnqueue`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/main/task-router.test.ts</automated>
  </verify>
  <done>The active app has a real task-router implementation and IPC bridge covering all missing router requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Add a renderer override panel for explicit agent/team routing</name>
  <read_first>
    - apps/desktop/app/src/renderer/App.tsx
    - apps/desktop/app/src/preload/index.ts
    - apps/desktop/app/src/common/task-router.ts
    - apps/desktop/app/src/renderer/components/AgentDetailPanel.tsx
  </read_first>
  <files>apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx, apps/desktop/app/src/renderer/components/TaskRouterPanel.test.tsx, apps/desktop/app/src/renderer/App.tsx</files>
  <action>Create `TaskRouterPanel.tsx` in the active app renderer with controls for `mode` (`auto`, `agent`, `team`), agent/team pickers, heartbeat interval input in milliseconds, current workload counts, and visible security fields `originatingSecurityLevel` and `effectiveSecurityLevel`. Mount it from `App.tsx` as a right-panel or modal-adjacent workflow seam without replacing the locked three-pane layout. The panel must call `window.clawhive.taskRouterEnqueue(...)` for task submission and `window.clawhive.taskRouterGetSnapshot()` for workload display. Use the exact labels `Auto Delegate`, `Specific Agent`, and `Team Collaboration` so acceptance can grep for them. Add a focused renderer test file that checks those labels and the presence of `originatingSecurityLevel` in the component source.</action>
  <acceptance_criteria>
    - `apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx` contains `Auto Delegate`
    - `apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx` contains `Specific Agent`
    - `apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx` contains `Team Collaboration`
    - `apps/desktop/app/src/renderer/components/TaskRouterPanel.tsx` contains `originatingSecurityLevel`
    - `apps/desktop/app/src/renderer/App.tsx` contains `TaskRouterPanel`
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/S/Projects/clawhive/apps/desktop/app && pnpm test -- src/main/task-router.test.ts src/renderer/components/TaskRouterPanel.test.tsx</automated>
  </verify>
  <done>User override routing is exposed in the active app UI with visible routing and security context.</done>
</task>

</tasks>

<verification>
The plan is complete when the main test suite proves router logic and the renderer test proves the override surface exists in the active app tree, with no implementation left only as comments or pending-task notes.
</verification>

<success_criteria>
- Heartbeat scheduling, auto-delegation, user override, workload balancing, and security propagation exist in `apps/desktop/app/src/`.
- The active renderer exposes a route-control surface rather than relying on hidden main-process behavior only.
- Router logic is backed by dedicated tests in the active app tree.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-agent-core/04-09-SUMMARY.md`
</output>
